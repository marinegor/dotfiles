- defaults:
  link:
    relink: true
    create: true

- clean: ["~"]

- link:
  ~/.bashrc: bashrc
  ~/.zshrc: zshrc
  ~/.config/nvim:
    - path: nvim
    - force: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]
