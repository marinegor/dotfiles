- cargo:
    - bottom:
        - locked
    - du-dust
    - exa
    - fd-find
    - jj jj-cli:
        - locked
        - bin
    - just
    - ripgrep
    - starship:
        - locked
    - xq
    - zoxide:
        - locked
    - pixi:
        - git: "https://github.com/prefix-dev/pixi.git"
        - locked
    - quickenv:
        - locked
    - hyperfine:
        - locked
    - dysk
    - agg:
        - git: https://github.com/asciinema/agg
    - bat:
        - locked
    - hwatch

- shell:
    - source ~/.bashrc && bash -c 'for t in pre-commit ruff basedpyright gpustat ty go-task-bin asciinema magic-wormhole; do uv tool install $t; done'
    - source ~/.bashrc && bash -c 'uv tool install --python 3.13 glances'
