- apt:
    - build-essential
    - curl
    - gh
    - libffi-dev
    - libgdbm-dev
    - libncurses5-dev
    - libnss3-dev
    - libreadline-dev
    - libssl-dev
    - wget
    - zlib1g-dev
    - zsh

- shell:
    - [chsh -s $(which zsh) $(whoami), Making zsh the default shell]
