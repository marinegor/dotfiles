- apt:
    - build-essential
    - curl
    - direnv
    - gh
    - git-crypt
    - libffi-dev
    - libgdbm-dev
    - libncurses5-dev
    - libnss3-dev
    - libreadline-dev
    - libssl-dev
    - unzip
    - wget
    - zlib1g-dev
    - zsh

- shell:
    - [chsh -s $(which zsh) $(whoami), Making zsh the default shell]
    - [
        owner=marinegor && sudo chown $owner -R .,
        Changing ownership of dotfiles,
      ]
